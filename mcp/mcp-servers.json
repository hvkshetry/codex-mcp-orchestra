{
  "mcpServers": {
    "office-assistant": {
      "command": "codex-custom",
      "args": ["mcp"],
      "env": {
        "CODEX_HOME": "${OFFICE_AGENT_HOME:-${HOME}/admin/.codex}"
      }
    },
    "openbb-analyst": {
      "command": "codex-custom",
      "args": ["mcp"],
      "env": {
        "CODEX_HOME": "${FINANCE_AGENT_HOME:-${HOME}/finance/.codex}"
      }
    },
    "router": {
      "command": "codex-custom",
      "args": ["mcp"],
      "env": {
        "CODEX_HOME": "${ROUTER_AGENT_HOME:-${HOME}/.codex}"
      }
    }
  }
}